<h1>New conf</h1>

<% form_for(@conf) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :configtext %><br />
    <%= f.text_area :configtext %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', confs_path %>